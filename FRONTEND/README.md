
  # Build Skoda-Volkswagen React Frontend

  This is a code bundle for Build Skoda-Volkswagen React Frontend. The original project is available at https://www.figma.com/design/676CfQPg3ad5iilWyc4oXi/Build-Skoda-Volkswagen-React-Frontend.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  